
import React from 'react';

const Header = ({ selectedNavItem, setSelectedNavItem }) => (
	<header className="site-header py-5">
		React Portfolio
	</header>
);

export default Header;

// Not sure how to insert the nav here